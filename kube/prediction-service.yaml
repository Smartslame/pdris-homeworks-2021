apiVersion: apps/v1
kind: Deployment
metadata:
  name: prediction-service-pod
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: prediction-service-pod
  template:
    metadata:
      name: prediction-service-pod
      labels:
        app: prediction-service-pod
    spec:
      containers:
        - name: prediction-service-pod
          image: pdris-homeworks-2021/prediction-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: prediction-service-balancer
spec:
  type: LoadBalancer
  selector:
    app: prediction-service-pod
  ports:
    - port: 8080
      targetPort: 8080