apiVersion: apps/v1
kind: Deployment
metadata:
  name: currency-service-pod
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: currency-service-pod
  template:
    metadata:
      name: currency-service-pod
      labels:
        app: currency-service-pod
    spec:
      containers:
        - name: currency-service-pod
          image: pdris-homeworks-2021/currency-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: currency-service-balancer
spec:
  type: LoadBalancer
  selector:
    app: currency-service-pod
  ports:
    - port: 8080
      targetPort: 8080